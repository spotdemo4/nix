
[global]
    e2dsa  # enable file indexing and filesystem scanning
    e2ts   # enable multimedia indexing
    ansi   # enable colors in log messages (both in logfiles and stdout)    
    ver    # show copyparty version in the controlpanel

    xff-src: 10.10.0.0/16
    xff-hdr: x-forwarded-for
    rproxy: -1

    dbpath: /db
    
    name: trev.zip
    spinner: ,padding:0;border-radius:9em;border:.2em solid #444;border-top:.2em solid #fc0

# get accounts config from decrypted age file
% @accounts@

[/]                  # create a volume at "/" (the webroot), which will
    /w/public        # share /w (the docker data volume)
    accs:
        r: *         # everyone gets read access
        rwmda: trev  # the user "trev" gets read-write-move-delete-admin
    flags:
        ui_nolbar
        ui_norepl
        ui_notree
        ui_nombar

[/dump]
    /w/dump
    accs:
        rw: *
        rwmda: trev
    flags:
        ui_nolbar
        ui_norepl
        ui_notree
        d2t
        dthumb
        vmaxb: 100g
        vmaxn: 4k
        lifetime: 3600
        maxn: 250,3600
        maxb: 5g,300

[/trev]
    /w/trev
    accs:
        rwmda: trev
    flags:
        ui_nolbar
