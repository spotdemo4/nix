name: garage_whitelist
description: Whitelist alerts for garage
whitelist:
  reason: ignore garage
  expression:
    - "evt.Parsed.traefik_router_name == 'garage-s3@redis'"
    - "evt.Parsed.traefik_router_name == 'garage-web@redis'"
